# 1
nodebrew setup

# 2
nodebrew install-binary latest
nodebrew use [target version]

# 3
# ここは、dotfilesで管理しているので不要 echo 'export PATH=$HOME/.nodebrew/current/bin:$PATH' >> ~/.bash_profile

# 4 load
source ~/.bash_profile
